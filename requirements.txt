# SQLAlchemy ORM 
SQLAlchemy>=2.0,<3.0

# Pydantic — validation/serialization layer in front of the ORM (see schemas/)
pydantic>=2.0,<3.0
email-validator>=2.0        # required for pydantic's EmailStr

# Database migrations
alembic==1.19.1

# fastapi>=0.110,<1.0       # web framework, request/response bodies driven by schemas/*.py
# uvicorn[standard]>=0.29   # ASGI server to run the FastAPI app
